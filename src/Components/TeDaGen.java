package Components;

import Components.OutputGenerators.IOutputGenerator;

/**
 * Created by bjornvangompel on 07/03/2017.
 */
public class TeDaGen {

    /**
     * Generate
     *
     * @param pattern    The test data generation pattern
     * @param iterations The Number of iterations to generate
     * @return returns the output in the form of an output generator
     */
    public static IOutputGenerator generate(String pattern, int iterations, IOutputGenerator output) {
        /** generationTime // Total generation time
         startStamp // Start time of the script
         log // Array containing the logs (warnings, etc)
         pattern // The data generation pattern to start with
         iterations; // Number of iterations to generate
         int i; // current iteration in iteration loop (do not use "i" in any other function!
         //matches // The regex matches array
         matchi // foreach loop counter in Matches **/

        boolean error;

        return output;
    }
}
